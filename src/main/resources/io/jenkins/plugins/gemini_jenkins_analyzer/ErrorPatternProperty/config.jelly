<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry title="Error Patterns for AI Analysis" field="errorPatterns"
             description="Regular expressions to filter error logs (one per line). These patterns will be used when analyzing errors from the console or using the analyzeError() step without patterns.">
        <f:textarea rows="15" style="font-family: monospace; width: 100%;" />
    </f:entry>
</j:jelly>
